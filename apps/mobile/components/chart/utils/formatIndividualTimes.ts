export function formatIndividualTimes(n: number): string {
  'worklet';
  return `${Math.trunc(n).toLocaleString()}`;
}

export function formatIndividualTimesNoUnit(n: number): string {
  'worklet';
  return `${Math.trunc(n).toLocaleString()}`;
}
