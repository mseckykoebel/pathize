{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:base"],
  "schedule": ["on the first day of the week at 6:00am"],
  "timezone": "UTC",
  "labels": ["dependencies"],
  "ignoreDeps": [
    "nvm, gradle, bundler, cocoapods, dockerfile, ruby-version, github-actions, gradle-wrapper, pip_requirements, yarn"
  ],
  "packageRules": [
    {
      "managers": ["npm"],
      "packagePatterns": ["*"],
      "groupName": "all npm packages",
      "minimumReleaseAge": "7 days"
    }
  ]
}
